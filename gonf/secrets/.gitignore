# Controller-local inputs for api.MustSecret and api.OptionalSecret.
# Keep the directory so a new checkout has the required recipe-root layout,
# but never commit credentials.
*
!.gitignore
!README.md
