apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pkgrepo-ingress
  namespace: infra
  annotations:
    spec.ingressClassName: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: pkgrepo.f3s.buetow.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pkgrepo-service
                port:
                  number: 80
