apiVersion: v1
kind: Service
metadata:
  labels:
    app: goprecords
  name: goprecords-service
  namespace: services
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: goprecords
