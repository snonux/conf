apiVersion: v1
kind: Service
metadata:
  labels:
    app: bgtutor
  name: bgtutor-service
  namespace: services
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: bgtutor
